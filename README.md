# Dota2-Stats-Parser
A simple Python script to fetch player statistics using the OpenDota API.
### Setup:
1. Install `requests` library.
2. Run `main.py` with your Steam ID.
*Future update: Add match history analysis for the last 20 games.*
